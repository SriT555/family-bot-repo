{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.py) black \"$f\" 2>/dev/null || ruff format \"$f\" 2>/dev/null || true ;; esac }",
            "timeout": 30
          }
        ]
      }
    ]
  }
}